{
  "devDependencies": {
    "ts-node": "^10.9.2"
  }, 
  "name": "thinkleap",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  }
}
